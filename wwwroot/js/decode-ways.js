/**
 * @param {string} s
 * @return {number}
 */
var numDecodings = function(s) {
    // dp[x] is the total ways. dp[x] = 
};

document.getElementById("decode-ways").innerHTML =
    numDecodings("12");