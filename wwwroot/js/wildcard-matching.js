/**
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
var isMatch = function(s, p) {
    
};

document.getElementById("wildcard-matching").innerHTML =
    // isMatch("aa", "a");
    isMatch("aa", "a");
