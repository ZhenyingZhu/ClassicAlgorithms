using System;
using System.Collections.Generic;
using System.Text;

namespace Algorithms
{
    /// <summary>
    /// Bisect two squares each in half with one line. Top and bottom sides of the squares are parallel to x-axis.
    /// Uncertainties:
    /// 1. How to represent two lines?
    /// 2. What if two squares exactly overlap?
    /// </summary>
    /// <remarks>
    /// CC189 6th 16.13
    /// </remarks>
    public class BisectTwoSquares
    {
        // Solution 1: Find the mid points of two squares and line them up. If use slope, need to consider the edge case when middles are on the same line
    }
}
