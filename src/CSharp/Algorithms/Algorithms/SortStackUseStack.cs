using System;
using System.Collections.Generic;
using System.Text;

namespace Algorithms
{
    /// <summary>
    /// Sort a stack use another temp stack. The smallest item is on the top.
    /// Uncertainties:
    /// </summary>
    /// <remarks>
    /// CC189 6th 3.5
    /// </remarks>
    public class SortStackUseStack
    {
        // Solution 1: use a var to store the pop result of the stack. In the other stack, pop elements out until find a bigger or equal element. Then push the var in.
    }
}
