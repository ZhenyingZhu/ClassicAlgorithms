using System;
using System.Collections.Generic;
using System.Text;

namespace Algorithms
{
    /// <summary>
    /// Given two line segments with start and end points, compute the intersection if any.
    /// Uncertainties:
    /// 1. what if no intersection? 
    /// 2. if they are two parallel lines?
    /// 3. if they are same line?
    /// </summary>
    /// <remarks>
    /// CC189 6th 16.3
    /// </remarks>
    public class TwoLinesIntersection
    {
        // Solution 1: use math. All the dots on a line can be represented by (Xa + x, Ya + x(Yb-Ya)/(Xb-Xa)). Xa + x is where the intersection. Then calculate.
    }
}
