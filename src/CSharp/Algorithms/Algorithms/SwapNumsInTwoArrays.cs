using System;
using System.Collections.Generic;
using System.Text;

namespace Algorithms
{
    /// <summary>
    /// In two arrays, swap two numbers to make them equal.
    /// Uncertainties:
    /// 1. what if more than 1 pair?
    /// 2. no result?
    /// </summary>
    /// <remarks>
    /// CC189 6th 16.21
    /// </remarks>
    public class SwapNumsInTwoArrays
    {
        // Solution 1: sum both array, find a pair of num with half the diff by sorting and two pointers.
        // Solution 2: use a hash table to find the diff.
    }
}
