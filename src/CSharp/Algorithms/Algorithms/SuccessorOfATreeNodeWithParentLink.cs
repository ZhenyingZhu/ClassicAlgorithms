using System;
using System.Collections.Generic;
using System.Text;

namespace Algorithms
{
    /// <summary>
    /// Find the next node under in-order traverse of a node in a tree. Each node has s link to parent.
    /// Uncertainties:
    /// </summary>
    /// <remarks>
    /// CC189 6th 4.6
    /// </remarks>
    public class SuccessorOfATreeNodeWithParentLink
    {
        // Solution 1: first find the right subtree left most node. If not have right subtree, then the right parent. If not right parent, then go up to the left most parent, then go right parent.
    }
}
