using System;
using System.Collections.Generic;
using System.Text;

namespace Algorithms
{
    /// <summary>
    /// Power set: return all subsets of a set
    /// Uncertainties:
    /// </summary>
    /// <remarks>
    /// CC189 6th 8.4
    /// </remarks>
    public class AllSubsets
    {
        // Solution 1: use DFS. Each element can be selected or not selected, so the total count is 2^n. Each element would need to be accessed, so time complexity is O(n*n^2-1)
    }
}
