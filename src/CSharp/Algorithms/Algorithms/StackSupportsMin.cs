using System;
using System.Collections.Generic;
using System.Text;

namespace Algorithms
{
    /// <summary>
    /// Design a stack that supports min. Push, pop, min should all be O(1).
    /// Uncertainties:
    /// 1. When stack is empty, what to return?
    /// </summary>
    /// <remarks>
    /// CC189 6th 3.2
    /// </remarks>
    public class StackSupportsMin
    {
        // Solution 1: Use another array to store the current min for each element.
        // Solution 2: Only store the current min once. If there are multiple elements are min, store it multiple times.
    }
}
