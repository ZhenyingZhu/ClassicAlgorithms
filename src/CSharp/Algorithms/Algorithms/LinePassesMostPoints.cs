using System;
using System.Collections.Generic;
using System.Text;

namespace Algorithms
{
    /// <summary>
    /// Find a line passes most points.
    /// Uncertainties:
    /// 1. what if points are all collpase?
    /// </summary>
    /// <remarks>
    /// CC189 6th 16.14
    /// </remarks>
    public class LinePassesMostPoints
    {
        // Solution 1: cross each two points, and hash the start point and slope. The slope might not exactly match so need to check if they are within an epsilon.
    }
}
