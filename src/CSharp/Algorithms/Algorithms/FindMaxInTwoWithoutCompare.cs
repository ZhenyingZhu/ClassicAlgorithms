using System;
using System.Collections.Generic;
using System.Text;

namespace Algorithms
{
    /// <summary>
    /// Find the max of two numbers, without using if-else or any comparsion operator.
    /// Uncertainties:
    /// 1. 
    /// </summary>
    /// <remarks>
    /// CC189 6th 16.7
    /// </remarks>
    public class FindMaxInTwoWithoutCompare
    {
        // Solution 1: use bit operation. Check if a - b > 0 by shift the result until sign bit. But a - b can overflow, so also need to check if a and b has different sign.
    }
}
