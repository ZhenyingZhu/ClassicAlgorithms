using System;
using System.Collections.Generic;
using System.Text;
using Algorithms.Utils;

namespace Algorithms
{
    /// <summary>
    /// Validate if a tree is a binary search tree.
    /// Uncertainties:
    /// 1. have dup values?
    /// </summary>
    /// <remarks>
    /// CC189 6th 4.5
    /// </remarks>
    public class ValidateBST
    {
        // Solution 1: recursively validate each node and its subtree.
        // Solution 2: in-order traverse but cannot handle dup values.
    }
}
